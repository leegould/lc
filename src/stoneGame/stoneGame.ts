/**
 * Stone game
 * @param input the list of stone piles
 * @returns true if the first player can win, otherwise false.
 * @description runtime O(1)
 */
export function stoneGame(input: number[]) {
    return true;
}